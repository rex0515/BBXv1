# BBX Documentation

BBX documentation is created by a script that reads the doc strings in the payload file and creates a markdown file with functions and their descriptions. It also alerts when there is a conflict in function names.

## Usage

Use `$ pip install -r requirements.txt` to install the requirements for the script.

After that you can run the `main.py` file which will create the markdown file at the build folder.


