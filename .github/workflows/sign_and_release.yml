name: sign and release

on:
  workflow_dispatch:
  push:
    paths:
        - 'src/manifest.json'

jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v5

      - name: Add to the latest release
        run: python3 /home/batuo/bbx/update.py
      
